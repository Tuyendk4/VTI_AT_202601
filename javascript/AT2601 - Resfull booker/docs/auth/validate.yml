info:
  name: validate
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/validate"
  body:
    type: json
    data: |-
      {
        "token": "QYjEYyiiXpjFAuJk"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
