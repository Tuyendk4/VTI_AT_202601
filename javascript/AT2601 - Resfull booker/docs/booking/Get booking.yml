info:
  name: Get booking
  type: http
  seq: 1

http:
  method: GET
  url: "{{bookingUrl}}/booking/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
