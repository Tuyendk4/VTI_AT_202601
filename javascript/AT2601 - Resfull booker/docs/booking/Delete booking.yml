info:
  name: Delete booking
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{bookingUrl}}/booking/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
