info:
  name: LGE2E005 - validate token again after logout successfully
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/validate"
  body:
    type: json
    data: |-
      {
        "token": "{{token}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: // let token = pm.collectionVariables.get("token");
    - type: after-response
      code: |-
        test("Status code is 403", function () {
            expect(res.getStatus()).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
